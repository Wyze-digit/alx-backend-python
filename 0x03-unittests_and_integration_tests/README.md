# Unit Test for access_nested_map

## Description
This project contains a unit test for the `access_nested_map` function located in the `utils` module.  
The purpose is to verify that the function correctly retrieves values from nested dictionaries based on a given path.

## Requirements
- Python 3.7+
- Ubuntu 18.04 LTS environment
- Follows `pycodestyle` version 2.5
- Each file is executable and ends with a newline
- Includes meaningful documentation for all modules, classes, and methods
- All functions are type-annotated

## How to Run Tests
```bash
python3 -m unittest test_utils.py

# Unit Testing for `access_nested_map` Function

## 🧠 Overview
This project provides comprehensive **unit tests** for the `access_nested_map` function located in the `utils` module.  
The goal is to verify both **successful data retrieval** from nested dictionaries and **exception handling** when invalid keys are accessed.

All code adheres to strict project requirements — including **PEP8/pycodestyle (version 2.5)**, **meaningful documentation**, **type annotations**, and **Python 3.7+ compatibility**.

---

## ⚙️ Environment & Requirements

- **Operating System:** Ubuntu 18.04 LTS  
- **Python Version:** Python 3.7  
- **Frameworks & Tools:**
  - `unittest` (built-in)
  - `parameterized` (for test parameterization)
- **Style Guide:** pycodestyle (version 2.5)

---

## Project Structure
advanced_api_project/
│
├── utils.py
├── test_utils.py
└── README.md
